[package]
name = "lmp-mcp"
version = "0.1.0"
edition = "2021"
repository = "https://github.com/lendmind-protocol/LMP"
homepage = "https://lmp-six.vercel.app/docs/guides/mcp-integration"

[dependencies]
anyhow = "1.0"
lmp-core = { path = "../lmp-core" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
