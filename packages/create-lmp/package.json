{
  "name": "create-lmp",
  "version": "0.1.0",
  "repository": { "type": "git", "url": "https://github.com/lendmind-protocol/LMP.git", "directory": "packages/create-lmp" },
  "homepage": "https://lmp-six.vercel.app/docs/getting-started",
  "type": "module",
  "description": "Initialize Lending-Mind Protocol in a workspace.",
  "license": "Apache-2.0",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "prepack": "pnpm build",
    "test": "node --test bin.test.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "files": ["dist", "README.md", "profiles"],
  "bin": {
    "create-lmp": "dist/bin.js"
  },
  "engines": {
    "node": ">=22"
  }
}
