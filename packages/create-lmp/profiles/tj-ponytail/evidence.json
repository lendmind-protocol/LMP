{
  "status": "verified-fixtures",
  "tests": [
    { "id": "minimal-native", "kind": "positive", "description": "A small native handler with no prohibited dependency passes the profile.", "expected": "pass" },
    { "id": "heavy-dependency", "kind": "negative", "description": "A prohibited heavy dependency is reported with a remediation path.", "expected": "needs_revision" },
    { "id": "runtime-semantics", "kind": "exception", "description": "Runtime behavior not represented by static checks remains a bounded human review item.", "expected": "blocked" }
  ],
  "notes": "This is a community archetype summarized from public documentation; it is not author endorsement."
}
