# Node Onboarding Safety

Node onboarding, template rendering, non-overwrite behavior, and package resolution. Apply only to the declared scope. Static checks are bounded evidence, not proof of universal correctness. Record failed, blocked, unsupported, and review-required outcomes.
