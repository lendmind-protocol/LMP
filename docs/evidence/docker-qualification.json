{
  "artifactVersion": "1.1",
  "status": "complete",
  "scenarioCount": 39,
  "expectedScenarioCount": 39,
  "dockerRequired": true,
  "dockerEvidence": [
    {
      "check": "selected-path-mount",
      "image": "lmp-sandbox:local",
      "network": "none",
      "exitCode": 0,
      "timedOut": false
    },
    {
      "check": "signed-mind-validation",
      "image": "lmp-sandbox:local",
      "network": "none",
      "capabilities": "dropped-all",
      "filesystem": "read-only",
      "exitCode": 0,
      "stdout": "valid",
      "timedOut": false
    }
  ],
  "passed": [
    "clean production-style source passes",
    "prohibited dependency is rejected",
    "TypeScript explicit any is rejected",
    "TypeScript eval is rejected",
    "TypeScript console output is rejected",
    "TypeScript dynamic require is rejected",
    "invalid Rust syntax is reported",
    "valid Rust parses",
    "mixed Rust and TypeScript workspace is evaluated",
    "unsupported source languages are reported explicitly",
    "empty workspace produces a pass",
    "non-source workspace is bounded",
    "generated and dependency directories are excluded",
    "invalid signature blocks evaluation",
    "missing policy is rejected by validation",
    "artifact is persisted with privacy metadata",
    "compiled profile layers are content addressed",
    "binary profile cache is created",
    "profile compilation is deterministic",
    "Git delta includes modified files",
    "Git delta includes untracked files",
    "Git delta excludes unchanged committed violations",
    "non-Git workspace falls back explicitly",
    "oversized source is bounded",
    "symlinked source is not traversed",
    "advisory mode is accepted",
    "audit mode is accepted",
    "MCP initialize handshake works",
    "MCP exposes protocol tools",
    "MCP rejects invalid JSON-RPC",
    "daemon watches created, changed, and removed workspace paths",
    "OSS fixture evaluates through the real CLI",
    "5,000-file million-line brownfield scan completes",
    "32 parallel profile definitions remain isolated",
    "Docker sandbox mounts only selected changed paths",
    "compiled Rust CLI validates the signed Mind inside the network-disabled Docker sandbox",
    "bounded fleet graph validates with deterministic order",
    "bounded fleet reports merge to a passing run",
    "fleet evidence disagreement blocks a passing decision"
  ]
}
